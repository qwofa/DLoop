# 变更记录

## 3.8.1 - 2026-09-16

- 安装器统一保护 Windows 外部命令参数，修复 SVN 项目路径含空格和短横线时被拆成选项的问题；Git 共用同一规则，保留路径末尾反斜线。检查失败时显示目标路径、退出码和原始错误。
- 安装包单独由 `installer-v3.8.1-r1` 标签冻结，一行入口同步指向修正版；原 `v3.8.1` 标签及工作流载荷保持不变。增加特殊字符路径的安装、校验、重装、卸载、失败恢复以及 PowerShell 5.1/7 参数传递回归。
- 基于冻结版 v3.8.0 及安装输出编码修正，补充生成器预计写入清单与开工授权覆盖检查。
- UI 采集产物统一进入当前版本管理目录，内部模型作为证据校验，真实产品越界继续阻断。
- 重试前检查原范围外变化，保留生效契约和身份；修正内部材料、重复启动及 UI 校验指引。
- 正式候选尚不可用时复用现有内部草稿发布，不新增人工 HTML 或完整成果恢复机制。
- 本版状态为 `frozen`，由 `v3.8.1` 标签固定；未部署使用者项目。冻结前独立复验 33 项全部通过，真实 Unity 编辑器采集仍未验证；详见[版本说明](docs/v3.8.1-friction-fixes.md)。

## 在线安装入口修正 - 2026-09-16

- 修复 Windows PowerShell 使用本地编码输出中文、在线入口按 UTF-8 解码而产生乱码的问题；安装及失败提示统一使用 UTF-8 子进程输出，编码设置不影响调用者终端。
- 补充本地中文编码与 UTF-8 环境中的真实一行安装回归，覆盖中文成功提示、失败提示及包含中文、空格和单引号的项目路径。
- 本次只更新在线安装入口和验证，继续安装冻结的 v3.8.0 载荷，不改写已发布标签。

## 3.8.0 - 2026-09-16

- DloopUI 增加四类必要材料核对和开工确认；缺项、歧义、未确认及确认失效均阻止实施。
- 用户确认按业务需求列明预制体、协议、配置和需求依据；恢复与返修沿用同一检查。
- 基于冻结版 v3.7.6，保留版本交替安装能力；发行状态为 `frozen`，由 `v3.8.0` 标签固定。
- 纯用途措辞修订可明确声明为非语义变更并保留原确认；缺项等待期间仍可维护调查资料，材料齐备后可维护设计和计划。
- 修正工具说明中的旧批准措辞，明确交互初稿无需单独批准，实施必须取得当前开工材料确认。
- 冻结前本地全量回归：核心 423 项（422 通过、1 项按源码环境跳过），发行安装 47 项（46 通过、1 项因未配置 Unity 编辑器跳过），无失败。详见 [3.8.0 开工材料核对](docs/v3.8.0-ui-readiness.md)。

## 3.7.6 - 2026-09-16

- 版本交替时整体归档并使旧作业失效，包含未完成作业和工作区占用；新版材料按完整发行版本隔离。
- 保留旧正文、附件、快照、摩擦记录和项目改动，安装失败恢复旧现场；同版本重装不归档，拒绝降级。
- 基于冻结版 v3.7.5，发行状态为 `frozen`，由 `v3.7.6` 标签固定。
- 详见 [3.7.6 版本交替](docs/v3.7.6-version-handoff.md)。

## 3.7.5 - 2026-09-15

- 删除无消费的 UI 配置摘要和检查详情，初始化、调研与发布只生成实际使用的校验报告，保留公开统计和最终验收检查；移除独立交互批准的过时描述及开发历史指令。

- 继续清理仅显示帮助的旧 UI 启动入口、未使用参数和历史文件黑名单测试；状态查询复用同次档案读取，诊断明细按需计算。首页区分两种入口的确认时机，移除注释与帮助中的旧发行年代标签。
- 基于冻结版 v3.7.4，发行状态为 `frozen`，由 `v3.7.5` 标签固定。统一发行、Skill、Unity 包和快照版本号。
- 删除旧初始化参数专测及旧指标名称断言；归并 3.7.4 说明中的推演案例与验证记录，精简 3.7.5 重复过程记录。
- 删除未使用的 UI 同步链、工作区占用更新包装、截图文字折行代码及其专用测试，保留当前调研与发布流程使用的共用校验和事务处理。
- 删除已失效的 3.3.2 示例；修正初始化配置指引和失效文档引用，合并重复的计划覆盖要求，更新 UI 入口说明与默认任务。
- 承接公开主线已有清理，移除旧评测、历史开发材料和对应 CI 步骤；合并重复规则断言，删除旧命令别名专测，修正 UI 最终验收与交付材料说明。
- 测试入口支持文件、名称及业务/集成分层筛选，输出逐项耗时与最慢用例；普通业务测试隔离无关历史快照写入，保留真实仓库、UI、命令行及恢复集成验证。默认 CI 仍运行完整集合。

## 3.7.4 - 2026-09-15

- 基于冻结版 v3.7.3，发行状态为 `frozen`，由 `v3.7.4` 标签固定。统一发行、Skill、Unity 包与快照版本号，档案结构和执行命令不变。
- 补充按可验收结果拆分、共同依赖一次决策和计划整体自检；后续切片按需细化，重新切片后检查受影响场景的承接，最终评审核对跨切片业务结果。复用现有计划、契约和评审，不增加状态、评分或人工确认。
- 补足缺失预制体的处理指引：先查找、集中说明缺口、复用既有授权并继续独立工作；适配当前调研匹配与实施范围，不恢复旧版计划新增模型或审批。

本次本地复核：核心回归 417 项通过、1 项源码安装态检查按规则跳过；安装与发行回归 43 项通过、1 项真实 Unity 采集按规则跳过，无失败。未进行真实模型拆分或 Unity 业务交互验证。另在隔离 Git 项目完成安装、校验、卸载，并核对四个公开规则入口与源码一致；生产逻辑仅同步版本常量。

### 需求拆分场景推演

以下保留 3.7.4 作者依据规则完成的推演案例，用于说明拆分边界，不是独立模型实测或运行验收。

| 场景输入 | 本次推演的处理结果 | 核对结论 |
|---|---|---|
| 已定位设置页的一处错字，要求修正并检查显示 | 普通 Codex 任务直接修改；不建立 DLoop 档案，也不把修改和检查拆开 | 入口条件能保持小任务规模 |
| 新增收藏、列表查看和取消收藏；项目已有稳定存储接口 | “收藏并可在列表看到”形成可用闭环，再交付取消收藏；两项均包含所需界面、接线和验证；最终核对收藏后取消的连贯结果 | 可以按结果拆分，无需强制基础层任务 |
| 两个入口都修改同一份偏好设置，但数据归属尚未决定 | 先调查并在设计中明确权威数据与更新责任；再决定合并实现或分步交付，不能让两个实施者各自决定状态 | 共同依赖先于切片独立实施 |
| 详情页可能依赖未确认的接口字段 | 先调查接口事实；影响结果的字段仍未知时保留具体缺项，不能猜字段或将详情页从范围删除 | 结构性未知不会被任务清单掩盖 |
| 要求制作两个界面，只找到其中一个 Prefab | 对缺失目标记录调研结论；已授权的新界面在设计与实施范围中承接，独立工作继续，制作后才采集真实截图 | 缺资产不造成自动生成或范围丢失 |
| 原切片中的统计展示改由后续切片承接 | 计划保留原场景到新切片的对应关系，复查相关依赖；接受的历史成果沿用，最终检查统计与前置操作串联证据 | 重新切片不等于取消原承诺 |

## 仓库整理 - 2026-09-15

- 移除旧版本评测工具与报告、历史样例、开发过程文档和不再使用的检查材料；保留当前运行代码、Skill、安装入口和有效回归测试。
- 当前说明集中在 README、依赖、安装、CI 和本变更记录；同步移除废弃的 CI 评测步骤及文档引用。
- 本次为仓库维护，运行载荷和已发布的 `v3.7.3` 标签保持固定。

## 3.7.3 - 2026-09-14

- 基于冻结版 v3.7.2，发行状态为 `frozen`，由 `v3.7.3` 标签固定；本版补齐开源发布准备和仓库持续集成，不改变既有工作流行为，不迁移旧作业。
- 接入可随源码迁移的 GitHub Actions：Windows 核心流程与快照、安装与发行，以及独立凭据检查。缺少必要依赖、空测试集合及未登记的跳过均使检查失败；实际 Unity 环境验证单独列出。
- 新增 MIT 许可证与依赖说明，Unity MCP 按能力接入，不绑定具体实现或版本；安装与使用说明明确当前版本要求。
- 首次公开发行仅导入本版源码，不带入原开发仓库的 Git 历史；补充隔离 Unity 项目的安装、Skill 识别、MCP 采集、交互、HTML 与卸载实测。详见下方发行验证记录与 [CI 说明](docs/ci.md)。
- 安装器只管理当前版本，支持全新安装、同版本重装、校验与卸载；不支持跨工作流版本继续旧作业。

### 发行验证

2026-09-14 的首次发行完成以下检查：

- 云端核心与快照、安装与发行、凭据扫描均通过；核心 417 项通过、1 项源码安装态检查跳过，安装与发行 43 项通过、1 项缺少 Unity 环境跳过。
- 本地配置 Unity 2022.3.62f2c1 后，安装与发行 44 项全部通过，无跳过。
- 隔离 Unity 项目实际识别两项 3.7.3 Skill，通过 CoplayDev MCP for Unity 10.0.0 完成编译、真实运行模式按钮交互、Prefab 截图、HTML 展示与卸载；采集保持场景和 Prefab，卸载保留交付档案。该 MCP 组合是实测记录，不是版本约束。
- 匿名的一行安装、校验、卸载，以及源码包下载和 SHA-256 核对均通过。

隔离示例验证接入和交付材料链路，未执行真实模型驱动的完整需求实施及最终业务验收；不代表所有 Unity/MCP 组合均已验证。发行包见 [3.7.3 Release](https://github.com/qwofa/DLoop/releases/tag/v3.7.3)，后续检查见 [Actions](https://github.com/qwofa/DLoop/actions)。
