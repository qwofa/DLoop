# 变更记录

## 3.7.3 - 2026-09-14

- 基于冻结版 v3.7.2，发行状态为 `frozen`，由 `v3.7.3` 标签固定；本版补齐开源发布准备和仓库持续集成，不改变既有工作流行为，不迁移旧作业。
- 接入可随源码迁移的 GitHub Actions：Windows 核心流程与快照、安装与发行、评测工具回归，以及独立凭据检查。缺少必要依赖、空测试集合及未登记的跳过均使检查失败；实际 Unity 环境验证单独列出。
- 新增 MIT 许可证与依赖说明，Unity MCP 按能力接入，不绑定具体实现或版本；安装与使用说明明确当前开发版状态。
- 首次公开发行仅导入本版源码，不带入原开发仓库的 Git 历史；补充隔离 Unity 项目的安装、Skill 识别、MCP 采集、交互、HTML 与卸载实测。详见 [3.7.3 说明](docs/v3.7.3-open-source-ci.md) 与 [CI 及迁移说明](docs/ci.md)。

## 3.7.2 - 2026-09-14

- UI 任务包声明交付材料要求，候选提交实际文件并将路径交给独立评审；统一交付入口自动汇总已接受任务的材料，新增最终输入准备及按材料修订、复用机制。缺项和必要验证未通过时给出明确阻断，业务完整性仍由双向核对负责。

- 基于冻结版 v3.7.1，发行状态为 `frozen`，由 `v3.7.2` 标签固定。DloopUI 取消例行前置需求、架构和交互人工批准，保留内部调查与设计；实施与候选评审明确要求结合相关后端、代码和界面补齐有依据的交互，并完成需求与实际实现的双向核对。普通 DLoop 流程保持原有语义。
- 新增随载荷安装的固定 HTML 模板，统一生成首次与返修交付页；截图可放大、拖动，编号与文字双向选择，窄窗口上下排列。Markdown 与仅含编号的 SVG 从同一数据生成，不再把文字拼进图片。
- 每个操作独立记录条件、动作、反馈、结果与验证依据，隐藏、无截图或无法定位的交互继续文字展示。必要实现或验证未完成时不进入最终验收。
- 最终展示绑定已接受候选、需求、设计与证据；材料或实现改变后旧验收失效。DloopUI 需要真实用户最终验收后才封存，不再沿用前置交互批准。
- 仅支持当前版本，不迁移旧档案；本次不包含 Unity 截图架构改造或在线编辑服务。详见 [版本说明](docs/v3.7.2-ui-delivery.md)。

## 3.7.1 - 2026-09-11

- 按用户指定版本号，基于冻结版 `v3.7.0`，当前为 `frozen`，由 `v3.7.1` 标签固定，保留主分支公开文档与一行安装入口。
- 实现作业独立本地 Git 快照库，自动保存实施检查点、候选、评审、批准和交付状态；支持手动命名、阶段开始标记、历史列表及原始字节导出，SVN 项目也需要 Git。
- 支持恢复预览、回退前保存、阶段重做、全新执行身份及原切片完整差异保留；支持失败恢复、冻结作业派生新作业，安装卸载保留历史，完整清理明确列出并删除快照。本次冻结不包含用户项目安装。详见 [3.7.1 方案](docs/v3.7.1-stage-snapshots.md)。
- 修复受管依赖锁文件因 `.lock` 后缀漏存、漏恢复的问题；连续回退保留同一材料的正式拒绝决定，只有用户重新批准后才可恢复批准效力。
- 修复 SVN 受管目录删除、替换后的阶段恢复；独立 UI 流程在调查、计划编辑前保存阶段起点；相同契约及检查证据复用原结论与保存点，证据变化仍保存新状态。
- 修复源仓库提交后再次保存会绕过早期快照恢复限制的问题；累计恢复范围保留首次仓库基准，基准变化后仍可保存和导出，但不能原地恢复。
- 修复回退后合法扩大或缩小实施范围导致无法继续的问题；重新绑定当前授权范围的撤销基线，保留原范围完整差异及新增范围的修改前内容。重复检查点在人工保存后仍复用已有保存点。
- 修复 SVN 重命名往返恢复丢失移动关系及来源历史、删除项遗漏提交组的问题；先恢复本地移动再还原正文，完整受管的移动目录整体撤回，删除和缺失文件统一恢复归组。
- 修复 UI 初始保存点遗漏原始需求正文的问题；明确登记的来源作为只读输入保存与导出，清单关联原路径和导出文件，恢复不因来源登记而覆盖原文件。

- 修复正式资源目录中 `obj`、`Library`、`Temp` 同名子目录被当成缓存漏存的问题；缓存排除限定项目根目录，回归覆盖正文、元文件、二进制、导出和往返恢复。
- 一行安装入口与离线安装说明统一固定到 v3.7.1。

## 3.7.0 - 2026-09-11

- 基于冻结版 `v3.6.8`（标签提交 `v3.6.8`），当前为 `frozen`，由 `v3.7.0` 标签固定；新增 Git 项目支持，本次冻结不包含安装到用户项目。
- Windows 安装器支持 Git 普通仓库、worktree 和仓库内项目子目录，验证既有 `.scratch` 忽略规则，保留安装、校验与卸载事务。
- 实施与交付绑定 Git 修订、暂存状态及内容、未暂存和未跟踪文件；支持未首次提交与 detached HEAD，保留范围保护和候选过期校验。Git 跳过 SVN 归组，不自动暂存、提交或推送。
- 保留 SVN、人工回退、UI 交互确认和整体交付自动冻结；本版不跨子模块实施，变化的子模块需独立运行。
- 修复被 Git 忽略的已接受产品产物在重新生成后仍能沿用旧确认封存的问题；后续切片、状态展示和最终验收复核产物正文及删除结果，同一路径采用最后一次接受的结果，不扫描其他忽略缓存或改写暂存区。
- 修复被忽略产物可跨子模块实施的问题；启动前按子模块登记路径检查授权范围，拒绝写入子模块及覆盖其父目录的范围，保留子模块内独立作业与暂存区内容。

## 3.6.8 - 2026-09-10

- 整体交付完成后自动冻结，复用集成确认并保留真实人工验收状态；全部计划切片收敛后主动进入验证，未实施的计划项仍阻断封存，后续普通调试不重开档案。

- 基于冻结版 `v3.6.7` 源码提交 `历史提交已脱敏`，当前为 `frozen`，由 `v3.6.8` 标签固定；本次冻结不包含安装到目标项目。
- 补齐人工回退的协调入口：区分暂停保留、原范围返修、撤销本轮与已接受成果变更；复用现有公开操作，不增加回退状态机。
- 用户只需说明意图；协调者先停止受影响执行，按当前状态处理成果，再刷新方案、材料和失效批准。暂停不登记会恢复基线的中断，已接受候选及评审历史保持不变。

## 3.6.7 - 2026-09-10

- 基于冻结版 `v3.6.6`，当前为 `frozen`，由 `v3.6.7` 标签固定；本次冻结不包含安装到目标项目。SVN 项目通过 `sync-svn-changelist` 将代码、Unity 元数据、文档、截图、正式状态及分享产物归入同一提交组，组名沿用由策划文档标题概括的交付标题。
- 非 SVN 项目跳过整理；缓存和锁不纳管；目录操作单独列出；仅本地分组，不执行服务器提交。保留 3.6.6 的交互确认能力。
- 修复归组误收既有父目录元数据、移动无关分组及阻断切片撤回的问题；关联资源元数据含原有修改时补齐交付说明。

## 3.6.6 - 2026-09-10

- 基于冻结版 `v3.6.5` 修正 DloopUI 实施前缺少明确人工交互审核的问题，当前为 `frozen`，由 `v3.6.6` 标签固定；本次冻结不包含安装到目标项目。
- 复用阶段批准入口增加 UI 交互确认，绑定展示版本和用户回复依据；需求共识已展示完整方案时可原子合并确认，详细开发提示和机器 PASS 不视作批准。
- 新增或改变交互、目标缺失或不可见时，先展示场景、标注和缺项再等待用户；未确认、退回或失效会阻断实施与相关交接、候选处理和最终批准。
- 交互或缺项处理变化后重新确认；经正常确认的排版、重新截图和纯内部整理保留有效交互批准。纯复用且无跳过项不增加人工关口，普通 DLoop 保留原路径。
- 最终验收绑定当时的完整 UI 方案，交互变更后即使重新确认或转为纯复用，旧最终验收也不会自动恢复；未改变方案的重复发布与非语义排版保留有效批准。
- 工作流状态格式更新为 6，仅处理当前版本作业；不迁移旧档案，不引入独立审批系统或对话认证服务。

## 3.6.5 - 2026-09-10

- 基于冻结的 `v3.6.4` 开发，当前为 `frozen`，由 `v3.6.5` 标签固定；本次冻结不包含安装到目标项目。
- 业务场景与评审指引明确多显示位置、触发及不处理条件，回查原始来源；UI 标注 PASS 不作为业务完整证明。
- 评审角色和下一动作返回问题、验证文件的输入准备入口，生成正确目录、结构和提交参数，重复准备保留已填内容。
- 交付与分享汇总分别显示接受情况、候选证据、未验证边界和档案保留状态，不以冻结字段推断是否验收。
- 摩擦入口直接给出必需参数，补记外部工具实际返工；汇总代价与重复证据未知状态，并给出可选补记入口。验证文档集中保留后续接入清单。

## 3.6.4 - 2026-09-09

- 基于冻结的 `v3.6.3` 开发，当前为 `frozen`，由 `v3.6.4` 标签固定；本次冻结不包含安装到目标项目。
- 任务包准备与实施启动统一拒绝不支持的通配范围，提示填写具体文件或目录，保留既有产品越界与修改占用保护。
- UI 调研默认生成当前编辑器的截图请求，通过 `--capture-manifest` 接入实际结果并复用原有校验及发布事务；只有显式指定可执行文件才运行批处理，不再从环境变量自动选择批处理。
- 摩擦汇总直接呈现现有执行账本的熔断事实与当前切片状态；同一交付项的文档级命令自动归属失败和恢复记录。
- 补齐本地需求来源、同层文档关联、生成文件范围与工具档案职责的入口指引，不增加旧作业兼容或迁移机制。
- 修复 Markdown 中标注图只有框线的问题：SVG 内嵌真实 PNG，原图直接在文内展示，避免普通 PNG 链接被阅读器当作网址打开；补充成品展示验收与回归。
- 2026-09-09 补强四类业务结果遗漏：原意与复用去向核对、阶段性缺项承接、展示信息落点、状态变化触发链。交接自动补入未引用的批准需求，保留精确章节并贯穿实施、评审和返修；不增加批准点或业务覆盖状态机。
- 2026-09-09 真实编辑器复验发现未命名场景无法附加截图场景，改用独立预览场景并限定截图相机的场景；新增未命名、已保存无修改、已保存有修改三种场景保护与真实像素回归。

## 3.6.3 - 2026-09-07

- 修复最终门禁在默认状态中遗漏的问题：另一交付项占用工作区时，最终批准与冻结显示真实阻断且不再提示推进；占用释放后恢复下一动作。
- 协调者状态查询默认只投递统一交付视图与必要占用身份，移除外围重复信息和恢复快照；`--include-details` 按需展开完整状态、历史、基线与最终检查结果，两种模式保留原有检查。
- 精简下游角色上下文：保留完整机器检查与当前任务材料，不投递全局方案、其他切片结论和协调调度；交接字段说明不进入角色材料，实施写入范围只在任务包出现，额外劳动记录填写规则按需读取。
- 修复后续任务包草稿或就绪状态阻断当前实施、候选评审和返修的路由问题；优先定位修改占用对应的执行任务，多个待办仍保留协调选择。
- 普通检查点复用本次工作区与 SVN 防护快照；角色视图复用本次校验的档案、批准和执行状态，不增加跨命令缓存。
- 明确内部实现整理与公共合同变化的评审边界，实施、设计评审和候选评审直接取得一致约束。
- 基于冻结的 `v3.6.2`，按既有行为修复和规则澄清使用补丁版本；当前为 `frozen`，由 `v3.6.3` 标签固定，冻结不包含安装到目标项目。
- 范围补充与宿主验证策略仅记录待评估方案，尚未实现；恢复内容分离按用户反馈不推进。

## 3.6.2 - 2026-09-07

- 活动实施与返修可单独追加验证或提高强度；保留原身份、成果、租约与历史，旧最终检查点失效，生成草稿按契约版本隔离。
- 独立实施角色直接取得失败处理、检查点和风险升级规则；实施、设计与候选评审可围绕当前问题只读核对相关项目事实。
- 候选评审检查真实流程正确性和维护成本，设计错误明确退回；最终验收角色只提交建议。
- 交接准备默认返回简短就绪信息，显式展开时才返回完整角色内容；双方共用原有材料检查。
- 基线摘要与恢复内容由一次文件读取生成，重叠范围去重；不增加持久缓存。
- 增加实际编码对照及结果记录，区分局部实验与真实 Unity 项目交付。基于 `v3.6.1`，当前为 `frozen`，由 `v3.6.2` 标签固定，冻结不包含安装到目标项目。

## 3.6.1 - 2026-09-07

- 修正需求确认与最终验收的展示粒度：按可独立判断的业务场景组织现有正文，最终结果沿用需求场景名称并链接正式需求，优先展示变化、偏差与待决定事项。
- 新建档案直接引导人进入需求或验收入口；填写提示不创建虚构场景、空状态表或固定媒体要求，正式正文形成后移除提示。
- Unity 默认沿用已核实的 Prefab 表现；只在本次交互变化或影响业务结果时调查和展示相关状态，集成验证继续覆盖当前场景的实际影响。
- 展示材料按决定选择；规划截图与原型不能充当最终实现证据，不新增审批点、场景状态机或前端模板。
- 实施、返修与最终验收角色直接收到必要的场景要求，交接准备与下游独立读取保持一致，不依赖协调者转写。
- 基于冻结标签 `v3.6.0` 修正展示与填写引导，按补丁版本交付；当前状态为 `frozen`，由 `v3.6.1` 标签固定，冻结不包含正式安装到 Dev。

## 3.6.0 - 2026-09-07

- 任务包输入附带依赖、未知项和上下文材料的嵌套填写说明；切片方案由同一入口在规范目录生成草稿，带入当前批准关系，重复准备不覆盖已有编辑。
- 输入准备给出后续检查入口，方案检查给出绑定本次摘要的批准入口；角色材料复用输入准备与旁路记录入口。
- 保留自动异常生命周期，增加额外劳动观察和恢复说明；补记不关闭自动事件、不改变业务状态、不构成完成门禁。
- 只读摩擦复盘保留已恢复记录并关联证据入口，分享快照包含当前版本新记录；明确数据覆盖与投入统计边界，不建设自动计时或根因修复状态。
- 基于冻结的 3.5.0，当前状态为 `frozen`，由 `v3.6.0` 标签固定；冻结不包含正式安装到 Dev，不重新验收已有产品工程。

## 3.5.0 - 2026-09-07

- 增加交接前材料准备：程序从正式事实装配下游材料，缺项返回上游原上下文，材料就绪后协调者才启动下游。
- 实施启动、候选提交和返修启动在状态切换前检查完整材料；失败不消耗新身份、不提前交棒，也不丢弃当前成果。
- 前置准备与下游独立核对共用材料规则，返修返回完整原候选与重验范围；完整投影不重复持久化。
- 继续保留独立评审、审批和候选固定边界；不增加交接状态、后台调度、自动重试或跨版本兼容。
- 基于已冻结的 v3.4.5 开发，交接专项与全量回归通过；发行状态冻结为 `frozen`，由 Git 标签 `v3.5.0` 固定。冻结不包含向用户项目正式安装的授权。

## 3.4.5 - 2026-09-07

- DLoop 入口收窄为至少包含一个可独立验收实现切片的大型功能交付；不满足入口条件的事项不初始化工作流，各切片按实际产物验证，不按文档、命名或环境类别设置验证豁免。
- 删除零切片文档交付旁路：没有已接受实现候选时不再建议进入验证，验证阶段会路由回实施，最终评审角色视图和最终批准均被阻断。
- 最终评审与批准始终要求非空候选集合、唯一工作区和集成确认；当前版本状态校验拒绝历史空候选批准，不增加兼容或迁移分支。
- 删除观察模块及通用扩展分发层；实施工作区只接受 SVN，缺少或不可用时明确失败；全工作流恢复为单一修改租约。
- 熔断后只允许保持原契约和写入范围重试，或由人工恢复工作区基线后终止当前切片；前置、替代或重新划分工作统一通过修改方案并创建新切片表达。
- 修复首次实施前协调者缺少文档写入目标的问题；未开始实施的切片可显式终止并保留检查历史，换新切片后不再被旧记录阻断验收；同步设计说明与当前支持边界。
- 发行状态冻结为 `frozen`，由 Git 标签 `v3.4.5` 固定；冻结不包含向用户项目正式安装的授权。

## 3.4.4 - 2026-09-04

- 设计评审只受需求与设计材料的新鲜度影响，并校验当前已确认正文；常见带引号、列表符号、分隔符或说明后缀的待补充占位会稳定阻断，调查总览不再作为设计评审必读材料。
- 最终验收评审与最终批准共用候选集合、唯一工作区和集成确认绑定校验；多个工作区的已接受候选在进入最终验收前直接拒绝，集成确认文件严格限制为三个合同字段。
- 零切片交付仍沿用当前支持边界，但最终验收必须读取并绑定已确认的实施与验证记录，不再仅凭空候选集合进入评审。
- 角色视图调用必须显式声明当前动作；角色与动作冲突时直接拒绝，不再由角色静默覆盖调用方动作。档案与术语格式版本保持不变。
- 发行状态冻结为 `frozen`，由 Git 标签 `v3.4.4` 固定；冻结不包含向用户项目正式安装的授权。

## 3.4.3 - 2026-09-03

- 新增 `evals/dloop-behavior/` 行为评测框架，以隔离夹具和语义 trace 记录材料读取、动作、命令顺序、状态迁移、写入、停止点与交接；硬门槛、合规、效率和未运行状态由标准库 grader 自动判定。
- 场景、trace、执行来源和写入目标由 grader 单点严格校验；规范相对路径阻止目录跳转，熔断后任何后续业务命令均触发硬门槛失败。
- 建立 10 个公开场景和 2 个不参与针对性改写的保留场景，覆盖创建/恢复、活动身份、实施与独立评审、失败与熔断、清理授权、普通 DLoop/UI 分离、新鲜度和最终候选漂移。外部模型 dry-run 与实际 CLI 状态机测试分别报告，不把自报 trace 冒充真实集成执行。
- 常驻入口收敛为默认路径、动作路由、关键不变量与完成条件，并保留只读分享动作；实施参考合并重复的角色交接和执行步骤。确定性状态机、安全边界和 explicit-only 策略保持不变。
- 全量隔离行为评测 12 个场景全部通过，硬门槛无回归；发行状态更新为 `frozen`，由 `v3.4.3` 标签固定。冻结不包含向用户项目正式安装的授权。

## 3.4.2 - 2026-09-03

- 从 3.4.1 重新实现轻量 DloopUI：模型只保留需求、已有 Prefab、截图标注、跳过原因和证据，删除计划新增资产、组件能力、交互节点、视觉状态、导航、规划项、验收项与绑定表。
- 需求先形成可观察结论，再由明确匹配清单登记已有 Prefab；Prefab 不存在、匹配不唯一、截图失败或标注目标不可见时记录跳过并继续，不创建占位 Prefab，也不寻找替代资产。
- 新增随 Plugin 安装的 Unity 编辑器截图包。它只实例化请求中的已有 Prefab，输出原始截图与可见 UI 矩形，不读取事件绑定、组件业务能力或按钮视觉状态，不保存或改写产品 Prefab。
- 规划输出收敛为一份计划阶段实施附件 `04-plan/ui-annotation-plan.md` 和按 Prefab 生成的标注 SVG；DloopUI 实施任务包必须完整引用它。原始截图保存在调查目录并随只读分享快照导出。按下、按住、回弹等 Prefab 既有行为默认保留，只有需求明确要求时才允许标注改变；跳过项仍由常规设计与实施范围承接。
- 需求原文、截图、截图清单或封存模型变化时重新阻断；实现阶段修改产品 Prefab 不会让历史截图计划自动失效。安装、校验和卸载继续使用仅当前版本的事务合同。发行状态冻结为 `frozen`，由 Git 标签 `v3.4.2` 固定。
- 将五段 UI 内部流水线收敛为 `ui-investigate` 和 `ui-publish` 两个业务入口；调用者只提交需求匹配和截图判断，内部模型、稳定身份、证据、Unity 请求、导入、审计与派生产物清理由 module 负责。
- UI 模型直接升为当前格式 3，删除旧摘要识别和只读兼容；重新调研采用完整快照替换语义，自动撤销旧匹配、旧标注、旧跳过和不再生成的媒体。
- 最终计划直接展示需求描述、来源定位、真实 Prefab、原始截图入口、修改或复用结论与归并跳过清单；本版本冻结前曾安装到目标项目 项目做专项试运行，正式安装仍需具体项目授权。

## 3.4.1 - 2026-09-01

- 统一交付视图在需求阶段先复用真实文档和可选配置门禁，再区分机器命令、正式文档编辑与人工决定；已登记任务包可按标识直接完成契约检查和启动，推荐动作不再要求重复定位已有文件。
- 新增 `prepare-action-input`，按当前事实生成任务包、检查点和候选的精确 JSON 结构。模板只固定机械字段与当前验证集合，列表数量由实际大小切片决定；已有输入文件不会被覆盖。
- 新实施、返修、熔断恢复和独立评审身份由下一动作合同给出可读建议，实际执行时继续核对唯一性且不预占状态。检查点身份从输入中移除，由工具在成功记录时按执行内顺序生成；同一执行内相同业务事实、契约和工作区的响应丢失重试保持幂等。
- 无版本控制工作区的防护摘要忽略工作流自身的 `.scratch/dloop-v3` 受管档案，与 Git/SVN 忽略该目录的正常项目行为保持一致。档案格式与术语格式版本不变；发行状态冻结为 `frozen`，由 Git 标签 `v3.4.1` 固定，尚未正式安装到目标项目 项目。

## 3.4.0 - 2026-09-01

- 公开档案命令改为从项目内固定工具入口推导项目根，唯一使用 `.scratch/dloop-v3/outputs/`；调用者不再传入档案根或项目根，携带交付项的命令结果明确返回实际规范位置，全局命令保持原有响应结构。
- 协调者角色投影只对已开放、可写的当前阶段返回总览的精确规范写入目标，阻断和只读动作返回空目标，其他角色不取得正式文档写入目标；任务包、检查点、候选和评审材料只接受当前交付项的对应规范阶段目录，集成确认继续完全沿用最终验收原有的位置与内容错误合同，分享快照和其他外部副本不能驱动工作流。
- 新增同步 `export-share`，把 Markdown、功能清单、UI 模型、媒体证据、派生交付状态和可归属摩擦原子发布到 `.scratch/dloop-v3/shares/`；发布前再次核对来源状态，变化时放弃临时结果且不自动重试。快照带身份与摘要清单，不导出机器工作流状态，并明确标识为只读、不可执行；拒绝分享输入时直接返回来源身份和精确规范恢复目标。
- 路径推导和快照导出都在当前 Python 进程内完成，不启动后台任务，不轮询外部结果。发行状态冻结为 `frozen`，由 Git 标签 `v3.4.0` 固定；档案格式与术语格式版本不变，尚未正式安装到目标项目 项目。

## 3.3.9 - 2026-08-28

- 将 Prefab、资源和生成绑定证据改为规划取证时的历史观察；首次取证缺失仍阻断，证据形成后不再读取产品文件、比较 SHA-256、Unity GUID 或 Prefab Variant 身份，也不再因实施结果产生证据过期和已有资产证据级联错误。
- 需求与实施检查改为核对配置同步时封存的术语摘要、规范化 UI 模型摘要和计划审计结论，不再重跑会访问产品文件的 UI 规划审计；产品文件任意变化不会使 UI 规划、需求批准或后续切片失效，直接改写封存模型仍按所属阶段阻断。
- 增加完整交付流程回归，覆盖产品 Prefab 与生成绑定实施、最终检查点漂移保护、候选提交、独立评审和下一切片启动。
- 发行状态冻结为 `frozen`，由 Git 标签 `v3.3.9` 固定；档案格式与术语格式版本不变，尚未正式安装到目标项目 项目。

## 3.3.8 - 2026-08-28

- 删除宿主退出拦截、会话登记、执行登记、转换适配层及其专用审计入口；安装、校验和卸载不再读取或改写 Codex 项目配置，普通任务退出不改变业务状态。
- 活动棒次、执行身份、检查点、修改租约和候选继续由正式档案持久化；重新进入后从统一交付视图恢复，完成、失败和中断只通过显式业务操作登记。启动与检查点支持内容完全一致的响应丢失重试，变化后的同标识输入确定性拒绝。
- 禁止实施写入范围覆盖工作流受管状态，仅允许精确的稳定 UI 模型文件；非协调角色从角色合同推导动作并返回实际阶段与精确下一动作，任务目标、非目标、验收、验证和回滚只从切片契约生成一次投影。
- 摩擦日志改为稳定问题、尝试、预期门禁和自动关闭的追加式生命周期；内容评审退回与主动中断不再误记为工作流异常。删除人工关闭命令和 DloopUI 独立视图生成入口，阶段视图统一通过配置同步写入。
- 发行状态冻结为 `frozen`，由 Git 标签 `v3.3.8` 固定；档案格式与术语格式版本不变，尚未正式安装到目标项目 项目。

## 3.3.7-dev - 2026-08-24

- 将第一期 DloopUI 作为显式 `dloop-ui-v1` 配置接入 DLoop 的既有阶段、审批、暂停恢复和计划完成门；普通 `$dloop` 不生成 UI 专属产物，当前合同中的配置字段保持可选。
- `ui-model.json` 保持 UI 单一事实源，工作流状态只保存相对路径与摘要；七份模型视图进入需求、调查、设计和计划目录。术语改变会使原确认失效，计划审计阻断时不能开始实施。
- 可选配置通过四类通用配置接缝接入，当前只静态登记 DloopUI；核心负责调用、状态提交和阶段门，不为未来配置预建动态发现协议。初始 UI 模型、来源材料证据与数量字段由同一运行时定义，初始化立即保存完整摘要，不再维护模板副本或独立初始化入口。
- 一个 Codex Plugin 同时包装 `dloop`、`dloop-ui` 与共享运行时，插件载荷由模块清单驱动；安装器只管理当前版本，其他版本在读取受管内容前拒绝。
- 新增显式调用、双重收敛、暂停恢复、证据规则、静态配置接缝、Plugin 结构和统一安装生命周期回归；不读取、转换或迁移旧版本 UI 文档。本开发候选未创建标签，也未正式安装到 目标项目。

## 3.3.6-dev - 2026-08-19

- 当前版本的固定命令行入口和双停止事件启动器禁止在受管目录写入 Python 字节码缓存；全新安装、实际运行、校验和卸载不再要求人工清理运行产物。
- 完整卸载只额外接纳固定工具目录中、直接位于已登记载荷目录的 `__pycache__` 下的普通 `.pyc` 文件，并在原有回滚事务中清理；技能目录中的缓存、散落字节码、缓存子目录和其他未登记文件继续整体拒绝。
- 安装回归覆盖实际运行后零缓存、历史缓存清理与故障回滚，以及伪装缓存和越界缓存拒绝。

## 3.3.5-dev - 2026-08-18

- 常驻入口只负责把主协调者路由到目标阶段合同；冷读、设计评审、实施、候选评审、返修和最终验收分别声明完整的上游输出与下游输入责任，不向无关阶段投递统一六项清单。
- 正式材料保持业务事实源，角色视图只做当前棒次执行投影；实施、候选评审和返修的任务消息只传递交付项、角色与必需执行身份，不再复制目标、范围、候选、验证或返修事实。候选评审即时取得完整任务包和固定候选，返修即时取得原候选及失败项到重验范围的保守对应。机器阻断原样返回，视图形成后的业务缺项才按具体缺项退回。
- 设计评审和最终验收接入同一动态角色交接：设计评审在形成角色视图前只校验正式设计已经确认且不是待补充占位，项目核对路径和更简单方案按实际需要提供；最终验收一次取得全部候选及对应评审，只把集成确认文件路径作为必要定位输入。非协调角色只从自身合同取得材料，不分页、不复制前序对话，也不替用户记录阶段决定。
- 结构化输入按照当前执行操作的字段合同构造，不读取历史模板；正常写入复用事务内资格检查，确定性失败输入未按原因改变时不得重试。
- 正式返修交接必须包含原候选标识、完整问题集、允许范围和重验场景；角色视图再从不可变评审快照补齐原候选明细，不新增逐项处理状态。本版本不新增交接文件、机器状态、持久档案格式、独立档案校验器、异常分类或摩擦账本能力。
- 候选评审只强制独立结论，失败或未验证时才强制问题清单；负向或边界测试证据和不适用理由保持可选，动态角色投影与实际门禁使用同一合同。
- 将验证等级、升级条件和安装生命周期分流收敛到唯一策略参考；计划、实施、返修、最终验证和冻结复用同一结论，其他提示规则不再维护重复测试矩阵。
- 清理固定超时、运行期版本宣言和常驻命令状态枚举；普通实施以返修交接是否存在判定棒次，宿主停止保护只保留变更时的信任、重载与真实黑盒验收边界。
- 版本基于 3.3.4 的固定提交继续开发，保持 `development` 状态；未创建标签，也未正式安装到 目标项目。

## 3.3.4-dev - 2026-08-14

- 验证强度改由变更风险决定：局部实现使用定向测试，共享边界升级模块全量，项目全量只用于发布级、安装器或宿主集成、范围不明、合并后不可归因和跨模块回归。
- 最终批准改为绑定集成确认、已接受候选集合和最终工作区；确认文件不再伪装成测试执行报告，内容未变化时可以复用。
- 独立评审证据和“不适用”理由改为可选补充，不再成为所有通过评审的机械门禁。
- 候选与评审改由不可变评审快照单点持有，移除活动状态中的同步副本和一致性双写检查。
- 中间检查点允许只登记本轮实际运行的验证；候选提交前的最后检查点仍要求契约完整验证集合全部通过。
- 工作区状态改为修改租约集合：不同版本控制工作区可以并行，同一工作区保持串行；旧状态结构直接拒绝，不新增迁移分支。
- 安装器改为仅当前版本：保留安装、同版本幂等重装、验证、禁用、卸载与回滚，删除跨版本升级事务和升级测试。
- 核心测试命令改为进程内调用，删除自动填充评审理由和重复摘要实现，显著降低回归启动成本。
- 当前说明只描述 3.3.4 有效合同，历史能力留在变更记录，不再作为当前入口约束。

## 3.3.3-dev - 2026-08-13

- 固定可删除观察模块的物理提供者代次；同一交付项删除后即使按原路径恢复，也永久保持关闭，新交付项仍可独立启用。
- 为中性扩展清单增加通用布尔启用检查；关闭或检查失败时不加载提供者、不合并降级字段，确认启用后的提供者故障才使用静态降级。
- 建立与当前项目 3.3.2 安装隔离的开发发行源。
- 新增版本化切片方案检查与批准、不可变批准历史和唯一当前版本指针。
- 七个及以上切片时派生带类型关系图，确定性拒绝自引用、缺失引用、重复关系、前置循环、替代循环和混合循环，并返回可复现关系链。
- 新增从批准声明与切片状态实时重算的开始资格和直接等待原因，并把启动、熔断恢复及候选绑定到可复核方案历史。
- 切片启动不再建立或扩展隐式方案；无当前批准方案、目标不在方案内或资格不足时统一拒绝。
- 候选自身固定完整批准方案绑定并纳入候选摘要，提交结果和独立评审上下文均可直接复核；档案校验会重跑每份批准历史的大切片图语义。
- 一至六个切片的命令结果与持久状态保持无图、无图分析和无资格字段。
- 新增只理解清单、动作、参数、核心结果和交付项路径的中性后置分发器；扩展缺失时跳过，异常时只追加清单静态降级字段，不能覆盖核心结果。
- 新增可整体删除的默认观察模块与清单，在独立旁路目录固定单次激活选择，并按稳定身份原子覆盖契约修订和熔断恢复记录。
- 修订分类不使用字符串相似度猜测语义；恢复分类按实际适用的授权范围和前置验收事实执行失败优先、多原因单次计数。
- 最终验收从核心既有历史对账应存在身份，输出三类计数与明细；缺失、失败或损坏记录永久标记不完整和可能少报，不补造分类事实。
- 增加提供者异常、关闭、重复调用、永久健康、模块与清单删除以及核心状态、租约、候选和最终批准不变性回归。
- 收口常驻技能的切片方案动作路由，并在实施、评审和验证规则中明确方案与候选绑定、独立评审、最终验证及观察旁路不作为核心输入的边界。
- 安装器继续从发行目录自动枚举载荷；发行测试固定中性分发器、观察提供者、启用保护层和清单进入安装锁，并验证完整卸载逐项移除。
- `development` 仅表示隔离发布候选源；本版本尚未授权正式发布、创建标签或安装到当前项目及其他正式项目，安装测试只使用临时 SVN 项目。

## 3.1.0 - 2026-08-07

- 为冷读拒绝、评审返修重试和执行中断增加中央四类失败分类与尽力写入的最小脱敏摩擦时间线；正常成功零读取零记录，旁路故障不改变原命令结果。
- 安装、升级、安全禁用和完整卸载均保留 `.scratch/dloop-v3/friction-inbox/` 既有事故记录。
- 增加完整卸载模式，只移除安装锁能够证明归属且内容一致的受管载荷、Hook、安装锁与临时绑定，并保留交付档案和项目 `svn:ignore`。
- 安装生命周期从 3.1.0 开始；3.0.0 及更早版本在载荷、Hook、绑定结构读取和项目修改前明确拒绝，不提供迁移、卸载或升级适配。
- 后续发行版安装到 3.1.0 起的受支持低版本项目时，自动执行统一双停止事件 Hook 与 v3 绑定结构的事务式升级替换。
- 升级在移除旧绑定、旧 Hook、旧载荷以及写入新载荷、新 Hook、新安装锁的六个边界均可完整回滚，不保留半升级状态。
- 本地漂移、未知 Hook、未知绑定、无锁残留、非法受支持版本安装锁和降级请求在目标项目发生修改前拒绝。
- 发行版本更新到 3.1.0；档案格式与术语格式版本保持 2，严格工作流合同版本保持 3.0.0。

## 3.0.0 - 2026-08-07

- 从 2.0 严格发行基线收敛为唯一 3.0 交付合同，删除初始化配置选择参数及内部术语可选分支。
- 新档案、父会话绑定、执行绑定和停止审计使用独立的 `dloop-v3` 根；旧根、旧合同和跨版本安装直接拒绝，不读取、迁移、补全、降级或覆盖升级。
- Windows Hook 启动入口强制使用 UTF-8，并增加中文基础设施故障消息、注释和非法输入输出的回归保护。
- 保留新档案原子提交、安装事务回滚、修改租约、候选固定、独立评审和持续基础设施故障后的受控放行，因为它们属于一致性、数据安全或防止永久续轮的必要保证。

## 2.0.0 - 2026-08-07

- 以项目内已验收的 DLoop 1.0 严格合同为发行基线，统一工作流、档案根、会话绑定和停止保护到 2.0 独立命名空间。
- 删除旧生命周期命令别名、无功能迁移告警、PowerShell 会话绑定、旧版本测试命名和非标准安装位置回退。
- 不完整档案、缺失依赖版本、缺失停止事件类型和不可读角色材料直接失败，不再自动补全、猜测或扩大读取范围。
- 安装器只接受干净安装或同版本重装；跨版本覆盖升级直接拒绝，旧版必须先依据安装锁完整卸载。
- 保留新档案的原子提交、安装事务回滚、修改租约、候选固定与独立评审，因为这些属于一致性和安全保证，不是兼容或降级策略。

## 0.6.0 - 2026-08-06

- 为 `workflow-status` 与 `context-summary` 增加共享的即时派生 `delivery_view`，稳定给出阶段、门禁、唯一下一动作或人工判断点、已有结论与三项接手复核，不新增持久副本。
- 最终批准绑定稳定排序的已接受候选、独立评审摘要与最终工作区；候选、评审、集合或工作区漂移确定性使批准失效，旧批准不猜测迁移。
- 将技能唯一名称改为 `DLoop / $dloop`，关闭隐式调用；项目规则仅在用户显式调用后读取技能和执行资格检查，不再因任务规模、自然语言或档案路径自动进入。
- 安装器安全迁移受管 `0.5.0` 的旧技能目录，拒绝未知文件与本地漂移；旧载荷删除、新载荷、Hook 和安装锁处于同一回滚事务，并由故障注入覆盖。
- 保持档案格式版本、术语格式版本、公开命令、交付状态、人工确认点和安装锁文件名不变。

## 0.5.0 - 2026-08-06

- 将功能交付流的完整规则拆分为固定参考模块，增加按业务动作只读投递闭合规则集的入口，常驻技能只保留路由、核心合同和最小读取原则。
- 增加结构化任务包上下文合同和动作级紧凑摘要，严格区分整份读取与指定章节读取，并为任务包、摘要、候选和评审记录建立固定字节预算回归。
- 抽取父会话与子执行绑定的公共原子写入边界，补齐共享身份路径、执行绑定关闭和 Windows 派生索引权限继承回归。
- 将同一停止处理器同时注册到父任务与子任务停止事件；父任务继续执行完整档案审计，未绑定子任务直接允许，已绑定子任务只审计对应执行并在闭合后解除绑定。
- 加固停止输入边界：支持多个完整事件的稳定合并、末尾未完成帧、损坏输入的安全结构诊断和错误前最小审计信封恢复；持续基础设施故障明确报告后结束，不把损坏输入伪装成业务审计成功。
- 保持档案格式版本、术语格式版本、交付状态和复杂配置人工确认点不变；本版本是当前冻结安装副本的可重建源码基线，不包含后续统一交付视图、验证证据绑定或精简路径改造。

## 0.4.0 - 2026-08-04

- 为六个交付环节补充首要读者、入口核心问题和准确性基线，使入口能够独立承载当前结论和范围边界。
- 增加轻量写作规则，要求必要业务行为明确主体、触发、操作和可观察结果，并区分调查事实、设计决定、计划任务、实际变化与验证结论。
- 在现有需求共识、重大架构确认和最终验收前复用独立冷读；冷读只使用明确选择的少量材料，并暴露推断、缺失与歧义。
- 不新增档案格式、公开命令、文档或交付状态、固定记录及人工确认点；AI 冷读不替代最终人工验收。

## 0.3.0 - 2026-08-03

- 增加显式复杂交付配置；默认只维护六个交付环节总览和术语表，专题记录按实际需要增加；既有 v0.2 标准档案只读弃用，不提供迁移。
- 增加需求共识和最终验收两个常规批准状态；重大架构决定可以按需记录，批准锁定当前负责文档正文并在正文变化后失效。
- 增加简化修改任务包、文档级新鲜度、工作区级唯一修改租约；轻量处理和标准档案写入也必须先查询全局写入资格，只读调查不进入候选状态机。
- 增加基于真实工作区快照和全局版本控制变化摘要的最新候选、候选指纹和独立验收；授权范围外变化拒绝提交，提交后任意变化拒绝验收，失败后允许继续修正。
- 收紧切片首次启动和文档级输入门禁，禁止失败切片通过重新开始更换范围并复用旧快照；无代码改动的复杂交付凭已完成实施记录与验证记录进入最终验收，不伪造切片。
- 增加受控孤立租约恢复：只有没有对应切片、工作区恢复到执行前状态且用户明确授权时才能释放，不自动接管或恢复。
- 将公开入口收敛为六个业务事务，内部状态变更步骤不再作为公开命令；最终批准绑定已验收工作区的全局变化摘要，并在摘要变化后失效。
- 增加执行身份最小绑定与停止防漏；活动执行不能静默结束，结果提交后清理当前执行绑定，子执行不受无关父级文档阻断，失败或中断修改继续由租约和最终门禁阻塞。
- Token 计量因缺少稳定宿主数据接入路径不进入 v0.3 正式范围；执行身份唯一性由独立的已使用标识集合维护。
- 最终批准在新执行开始时失效，任何共享工作区修改租约都会阻止最终批准和冻结；冻结快照仍可只读查询工作流状态。
- 本版本候选不自动发布、打标签或安装；这些操作仍需专项授权，旧档案迁移能力不提供。

## 0.2.0 - 2026-07-31

- 增加最小只读 `audit`，只返回允许结果或事实 blocker，并确定性检查指定交付项的完整结构和全部正文确认状态。
- 增加父会话临时绑定、同会话切换、查询和幂等解除；登记文件名只保存会话身份摘要，正文只保存交付项标识。
- 增加项目级 Codex Stop 薄转换；确定性档案问题持续阻断，基础设施首次失败要求诊断，宿主续轮后仍失败则报告并放行；宿主输入损坏或缺少续轮标志时按首次故障处理。
- 未绑定会话警告后放行，不扫描或猜测其他交付项；Subagent 不创建或覆盖主协调 Agent 的登记。
- 安装器收窄为当前 Windows SVN 项目，只验证既有 `.scratch` 忽略规则，拒绝 JSON 或 TOML 中的未知项目 Hook、项目 Hook 关闭状态和无法安全处理的受管文件漂移。
- 增加事务式安装、只读验证、安全禁用和重新安装；失败时同时恢复文件和目录状态，安全禁用只移除精确归属的项目 Hook 并清理合法临时登记。
- 删除会话基线、项目指纹、绑定时间、适配器版本、整体档案指纹、机器动作、故障标记、Git/非 Windows 安装和第三方 Hook 合并等未获批准能力。

## 0.1.0 - 2026-07-29

- 建立独立的功能交付流 Git 仓库。
- 收纳功能交付流技能、档案 CLI、测试和发布元数据。
- 提供带版本校验、冲突保护、安装锁定和只读验证的项目安装脚本。
- 保持项目交付档案与工作流发行版分离。
