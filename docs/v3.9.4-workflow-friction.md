# v3.9.4 开发摩擦修复

状态：`frozen`。冻结日期：2026-09-24。基于公开主线 v3.9.3。已完成本地源码验证，尚未公开发布；目标项目升级单独执行。

## 范围

- 临时分析目录中的外部依赖连接不再阻断开工。只排除未纳管的 `.scratch/outputs/`，并在递归进入目录前裁剪；已纳管文件、目录属性及业务范围变化仍受检查。
- 输入准备返回正确的身份参数，缺失身份明确列为待填，不猜测或自动启动实施。正式输入路径错误返回规范目录和当前下一动作入口。
- Windows 常见的 UTF-8 BOM 可用于任务包、检查点、候选和开工清单；工具生成格式保持无 BOM UTF-8。
- 开工清单完整但内部需求未就绪时，指向已有文档补齐动作；材料齐备、用户批准、实际验证仍分别核对。
- Skill 使用工具返回参数和路径，登记配置及协议生成器的完整输出范围；缺少业务事实时集中说明，没有新事实时停止重复提交。

## 验证范围

- 真实 SVN 目录连接复现、临时父目录未纳管与已纳管两种情况、已纳管输出和业务文件保护、产品目录外部连接拒绝。
- 真实 Git 未纳管与已暂存文件区别；参数纠错后直接生成输入；带 BOM 输入完成任务包登记、检查点和候选提交；开工材料与内部文档状态分离。
- 完整核心集合 442 项、安装发行集合 56 项。首次运行中，核心及发行各有一项 Skill 入口长度检查失败；压缩指引后复验通过。编辑该文案期间另有一项安装回滚测试因源码摘要变化提前退出，固定源码后复验通过。其他用例全部通过。
- 核心源码安装归属检查与真实 Unity 采集各按既有环境规则跳过一项；实际安装归属由发行测试覆盖。未执行真实 Unity 业务操作。
- 复验覆盖完整上下文模块以及发行元数据和受影响的安装回滚用例；日志保存在源码工作区外。通用 Skill 校验器不识别现有宿主字段 `disable-model-invocation`，仅在临时校验副本中去掉该字段后通过，源码保留该字段并由发行测试验证。

## 使用边界

业务材料缺失、验证失败、活动切片关系冲突和缺少用户确认仍是有效阻断，不以放宽门禁解决。使用当前下一动作推进；实施关系确需改变时终止原切片并建立新切片，验证未通过时保持待验证。

项目升级仍将旧版本作业整体归档失效，不接续或迁移旧作业。已有活动作业应先明确结束或归档时机；禁止在已安装载荷上直接打补丁或改写安装锁。
