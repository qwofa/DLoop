# Project Rules

## 运行前提与版本边界

- v3.9.1 根据用户明确提出的旧安装本地补丁与新增内容升级问题，允许安装较新版本时备份并替换旧安装锁中已登记文件的内容变化；受管目录内新增文件和目录完整备份，不冲突的保留原位，占用新版安装路径的移出后继续安装，失败恢复原状。仍须完整校验安装锁归属、路径和已登记文件类型；新增内容不登记为新版载荷，同版本重装和卸载不放宽。本例外不扩展到业务作业文件的外部改写或旧作业接续。
- v3.7.6 明确支持版本交替：安装较新版本时，旧作业无论是否结束，都整体归档失效并解除活动工作区占用；原文档、快照和代码改动保留，新作业使用发行版本独立的活动目录。该授权包含旧安装的归属检查、替换及失败回滚，不包含迁移或继续执行旧作业；本条优先于以下跨版本排除规则。

- 只考虑 dloop 自身工作流产生并管理的代码、文档、档案和状态；假定这些内容在作业期间不会被人为修改，也不为其他外部越权改动增加检测、合并、恢复、冲突处理或防御性逻辑。
- 设计、实现、测试和审查只考虑正常 AI 工作流按照当前 Skill、命令和合同产生的修改；不考虑人工或外部进程直接改写、伪造或破坏受管文件才能触发的问题。
- 不得将上述越过正常 AI 工作流的直接改写场景列为缺陷、验收阻断或测试要求，也不得因此增加校验、兼容、恢复或防御性复杂度；除非用户在当前任务中明确扩展这一边界。
- 每个 dloop 作业从开始到结束始终使用同一版本的工作流、格式和规则，不存在跨版本继续、恢复或交接作业的场景。
- 当前版本只需正确处理由当前版本创建的作业及其产物；不得为读取、恢复、迁移或继续执行其他版本创建的作业而增加兼容逻辑或测试。
- 上述边界不削弱当前版本已经明确规定的完整性与安全保证，包括工作区变化检查、受管文件归属校验、档案结构校验和事务回滚。

## 审查与改动建议过滤

- 提出缺陷、验收阻断或改动建议前，必须先给出由当前版本正常 Skill、命令和合同触发的完整路径，包括触发者、合法输入、系统操作和错误结果。
- 如果问题必须依赖人工编辑受管文件、伪造工具产物、绕过公开命令、使用其他版本产物，或由外部进程破坏状态才能发生，则直接排除，不得报告为缺陷、风险、测试缺口或改动建议。
- 单元测试可以使用简化、模拟或不具备真实媒体内容的测试夹具。不得仅因测试夹具可以被构造、导入函数没有防御任意伪造输入，便推断正常工作流存在生产缺陷。
- 正常工具自身生成错误、不完整或与其合同不一致的结果仍属于审查范围，但必须由真实执行证据或当前生产代码中的确定路径证明，不能以人工构造输入代替。
- 来自子 Agent、审查 Skill、外部工具或通用最佳实践的发现，必须先经过本节边界过滤；与本节冲突的发现应当丢弃，不得为了保持原报告结构而继续呈现。
- 对范围外破坏缺少检测、防御、恢复或兼容能力，不属于实现不完整，也不得据此增加状态、校验、测试或文档。

## 向后兼容策略

- 本工程不保证向后兼容；除非用户在当前任务中明确要求，否则不得为旧版本保留兼容能力。
- 新版本可以直接调整文档结构、档案格式、元数据、命令行为和工作流，不需要保证旧版本生成的文档或档案能被新版本读取、迁移或继续使用。
- 不得仅为兼容旧版本增加格式探测、迁移逻辑、回退分支、双读双写、旧名称别名或兼容性测试；已有此类逻辑在妨碍当前设计时可以删除。
- 测试和文档以当前版本的格式与工作流为准，不以旧版本行为作为验收标准。
- 唯一必须持续有效的是当前版本的安装与卸载功能：安装必须能把当前版本正确部署到受支持的目标位置，卸载必须能完整移除由当前版本安装和管理的内容。
- 不要求当前版本兼容旧版本的安装状态、锁文件或卸载记录；如需处理旧安装，必须由用户在当前任务中明确提出。
- 本节规则优先于仓库内其他文档中的兼容、迁移或旧格式读取约定。

## Skill 版本与文件命名

- 开始新版本开发或解释当前行为前，先核对实际工作目录、分支、`VERSION`、`release.json` 与已发布基线；不得根据目录名称推断当前版本。新版本使用独立工作目录，稳定主目录保留已发布版本。
- 发布完成后核对远端主线，并在稳定主目录干净时同步本地主线与工作区。存在未提交成果时先保全并核对去向；历史无共同祖先时明确处理基线，不直接拉取合并或覆盖旧现场。
- 每次产出的 Skill，其描述中必须明确附带该 Skill 的当前完整版本号；不得只依赖目录名、发布清单、锁文件或其他外部信息表达版本。
- 整理新版本需求或创建新版本文件时，必须先依据当前发布计划确定符合语义化版本规则的目标版本号，并与 `VERSION`、`release.json` 和发布说明保持一致。
- 不得按需求批次数量机械递增补丁版本；不得复用或覆盖已经冻结或发布的版本号。
- 文件或文件夹名称在版本号之后仍有说明文字时，使用 `v<版本号>-<说明>` 的形式，例如 `v3.1.1-install-cleanup`。

## 对外材料

- 对外文档、Skill 指引和示例使用通用流程描述，不引用实际项目名称、业务经历或可识别的需求片段。
- 不写入个人邮箱、用户目录或本地实验路径；必要定位使用仓库相对路径或明确的通用占位。
